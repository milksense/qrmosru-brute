{
  "name": "qrmosru-brute",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "start": "cd ./packages/qrmosru-brute && yarn start",
    "clear": "lerna run --scope app clear"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.5.12",
    "lerna": "^4.0.0"
  }
}